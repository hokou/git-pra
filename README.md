# git-pra

## rebase

```
git rebase

git push origin -f
# force push to remote

git reflog
# Q to xeit

git tag <tagname>
git push origin --tags

del
git tag -d <tagname>
git push -d origin <tagname>
```
